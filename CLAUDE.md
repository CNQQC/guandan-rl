# 掼蛋研究室 · 项目约定

## 训练前端固定在 7860 端口

网页控制台（对战 / 训练 / 评测三个页签）**始终跑在 http://127.0.0.1:7860**。

```bash
.venv/bin/python -m guandan serve
```

- `guandan/__main__.py` 里 `serve --port` 的默认值就是 7860；README 和 `.claude/launch.json` 也按这个端口写。三处保持一致。
- 端口被占用时，**先确认占用者是不是本项目的旧控制台，是就停掉它再起新的**，不要改用 7861、7862 之类的临时端口。调试也不例外。
- 改动 `guandan/` 下的 Python 代码后必须重启该服务才生效；`web/` 下的 HTML/CSS/JS 是每次请求实时读盘的，刷新页面即可。
